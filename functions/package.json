{
  "name": "functions",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "npm run lint && npm run build && firebase deploy --only functions --project pad-champions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google/genai": "^1.38.0",
    "@modelcontextprotocol/sdk": "^1.25.3",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.0.3"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "eslint": "^9.39.2",
    "firebase-functions-test": "^3.1.0",
    "globals": "^17.1.0",
    "graphql": "^16.12.0",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.53.1"
  },
  "private": true
}
